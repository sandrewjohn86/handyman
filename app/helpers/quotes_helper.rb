module QuotesHelper
	# def create_or_edit_quote_path(project)
	# 	user_quote = current_user.project_quote(project) if user_signed_in?

	# 	if user_signed_in? && @user_quote.present?
	# 		link_to "Edit quote", edit_users_project_quote_path(project, user_quote)
	# 	else
	# 		link_to "Create Quote", "javascript:void(0)", onclick: "myFunction()"
	# 	end
	# end
end
