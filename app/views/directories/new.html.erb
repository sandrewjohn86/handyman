<h1>Directories#new</h1>
<p>Find me in app/views/directories/new.html.erb</p>

<h2>Create New Directory</h2>

<%= form_for @directory, url: directories_path do |f| %>
	<!-- Check for errors -->
	<% if @directory.errors.any? %>
		<% @directory.errors.full_messages.each do |msg| %>
			<!-- Show errors -->
			<div><%= msg %></div>
		<% end %>
	<% end %>

	<div>
		<%= f.label "Directory Title" %>
		<%= f.text_field :directory_title, autofocus: true %>
	</div>

	<div>
		<%= f.label "Description" %>
		<%= f.text_area :directory_desc, autofocus: true %>
	</div>

	<br>
	<%= f.submit "Submit"%>
	<%=link_to "Cancel", root_path %>
<% end %>
